dataset:
  dataset_name: DRIFT
  dataset_path: datasets/stream/synthetic/drift.xes
  end_events:
    - 'H'
    - 'N'
    - 'O'
  drift_points:
    - 500

transformer:
  type: index
  max_events: 10
  include_prefix_len: false

model:
  type: aht
  params:
    drift_detector:
      type: none

    grace_period: 100
    delta: 0.001
    tau: 0.05
    split_criterion: 'info_gain'

    leaf_prediction: 'nba'
    nb_threshold: 10

    switch_significance: 0.05
    max_depth: 20
    seed: 42

output:
  folder: experiments/outputs/streaming/drift
